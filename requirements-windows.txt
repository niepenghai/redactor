PyMuPDF>=1.23.0
pywebview>=4.0
pillow>=9.0.0
numpy>=1.21.0
dataclasses-json>=0.5.0
# spaCy dependencies - optional, installed via precompiled wheels in build process
# spacy>=3.4.0 is installed separately using --only-binary flag to avoid compilation issues